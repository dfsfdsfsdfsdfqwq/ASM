{% extends 'base.html.twig' %}

{% block title %}Brand{% endblock %}

{% block body %}

<div class="container">
    <h1>Brand</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
            <tr>
                <th>Origin</th>
                <td>{{ brand.Origin }}</td>
            </tr>
        </tbody>
    </table>
    <button class="btn btn-primary" >
        <a href="{{ path('app_brand_edit', {'id': brand.id}) }}"style="color:white;">Edit</a>
    </button><br>
    <br>
    {{ include('brand/_delete_form.html.twig') }}

    <button class="btn btn-primary" >
        <a href="{{ path('app_brand_index') }}"style="color:white;">Back to list</a>
    </button>




</div>
{% endblock %}
